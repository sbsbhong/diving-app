{
  "name": "@repo/contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "yarn generate",
    "generate": "yarn generate:ts && yarn generate:swift",
    "generate:ts": "node ./scripts/generate-typescript.mjs",
    "generate:swift": "node ./scripts/generate-swift.mjs"
  },
  "devDependencies": {
    "json-schema-to-typescript": "^15.0.4",
    "quicktype": "^23.2.6"
  }
}
